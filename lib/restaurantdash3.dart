import 'package:flutter/material.dart';

class RestaurantDash3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Stack(
        children: <Widget>[
          Container(color: Colors.white,
            child: Text("HOME3"),),
        ],
      ),
    );
  }
}
